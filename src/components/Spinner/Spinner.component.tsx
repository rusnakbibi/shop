import { SpinnerContainer, SpinnerOverlay } from './Spinner.style';

const Spinner = () => {
  return (
    <SpinnerOverlay>
      <SpinnerContainer />
    </SpinnerOverlay>
  );
};

export default Spinner;
